[oselab]
localhost

[OSEv3:vars]
# The course name used by repository name
course_name=ose;

# OpenShift Version
openshift_version=3.2

# User Lab uid
guid=5d78

# Repository Version
repo_version=3.2

# Install HA Environment
install_ha=False

# Install Load Balancer if install_ha is False automatically the install_lb will be false too
install_lb=True

# Install nfs server
install_nfs=True

# Install DNS Server
install_dns=True

# Use IDM for autentication, if not http_passwd will be enabled by default
use_idm=True

# Install metrics
install_metrics=True

# Install logging
install_logging=True

# Install demo projects
install_demo=True


# DO NOTHING HERE
[OSEv3:children]
oselab
lb
nfs
etcd
masters
nodes

[lb]

[nfs]

[etcd]

[masters]

[nodes]

